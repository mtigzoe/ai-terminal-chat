import React from 'react';

/**
 * Provider selection now lives on the dedicated Settings page.
 * Keep this component as a compatibility shim for existing imports.
 */
const ProviderSelector = () => null;

export default ProviderSelector;
